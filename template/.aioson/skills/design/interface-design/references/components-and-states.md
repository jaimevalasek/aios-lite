# Components and States

Every component family must define:
- default
- hover
- focus
- active
- disabled
- loading

Container components also need:
- empty state
- error state
- success confirmation when relevant

Forms must use external labels, visible focus states, and inline validation.
